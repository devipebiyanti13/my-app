pipeline {
    agent any
    stages {
        stage('Test Git Clone') {
            steps {
                sh 'git clone https://github.com/devipebiyanti13/my-app.git'
                sh 'ls -la my-app'
            }
        }
    }
}
